= render 'shared/error_messages', object: @service

= form_for @ticket do |f|
  = f.text_field :name
  = f.email_field :email
  = f.collection_select(:department_id, Department.all, :id, :name, include_blank: '')
  = f.text_field :subject
  = f.text_area :question
  = f.submit